hostname = '<%= @hostname %>'

# This should be the license_key to your Newrelic account
license_key = '<%= @nr_license %>'

# Name of your server instance
instance_name = '<%= @server_instance %>'

# Polling interval of the plugin (default 60)
poll_cycle = ''<%= @poll_cycle %>''
